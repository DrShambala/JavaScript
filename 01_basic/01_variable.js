//VARIABLES

const accId=12345   // it can not be change

let email= 'example@gmail.com'  // does not have block issue

var passwd= 'eample123' // does have block issue

city='umbrella' // not a good way of declaring variable

let state;      // an variable whose value is not defined
// it is not always necessary to use semi colon

console.log(accId);

// accId=2
// console.log(accId);

console.table([accId,email,passwd,city,state])
